[core]
  git-dir = /root/bird-auto-perf.git
  work-dir = /root/bird-auto-perf-work/
  log-dir = /root/bird-auto-perf-log/
  maxage = 3 years
  remote = origin
  cycle = 600

[db]
  db = perftest
  user = perftest
  pass = Jart6~swecket2

[test "filter"]
  name = BirdTest::Filter
  run = none
  run = reject
  run = false
  run = func
  run = rta
  run = df1
  run = df100
  run = eattr
  run = eatin
  run = eattr10
  run = eatin10
  run = eatin5
  run = assign
  run = net

[compiler]
  run = gcc-6
  run = clang-3.8
  run = clang-3.9
